# MongoDB config
MONGODB_SETTINGS = {
    'db': 'scm',

    # Defaults to localhost:27017
    # 'host': '<db hostname or ip>',
    # 'port': <db port number>,

    # Database login if required
    # 'username': '<db username>',
    # 'password': '<db password>'
}

DEBUG=True
